"""Renderizado de un `TriangleIndividual` a una imagen sobre canvas.

Pendiente: dibujar los triángulos (color uniforme, posiblemente translúcido)
sobre un canvas base usando una librería de imágenes (p. ej. Pillow).
"""
